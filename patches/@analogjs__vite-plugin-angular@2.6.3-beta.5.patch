diff --git a/src/lib/angular-vite-plugin.js b/src/lib/angular-vite-plugin.js
index 3a613018bac167eb5d9a52c3d2b8a233c2366329..c2b1340efe387c8dd93198a25151a8502ed79b43 100644
--- a/src/lib/angular-vite-plugin.js
+++ b/src/lib/angular-vite-plugin.js
@@ -134,6 +134,7 @@ export function angular(options) {
         }
         return {
             name: '@analogjs/vite-plugin-angular',
+            enforce: 'pre',
             async config(config, { command }) {
                 watchMode = command === 'serve';
                 isProd =
@@ -497,12 +498,16 @@ export function angular(options) {
                         await pendingCompilation;
                         pendingCompilation = null;
                     }
-                    const typescriptResult = fileEmitter(id);
+                    const lookupId = id.startsWith('/') && !id.startsWith('//')
+                        ? normalizePath(resolve(resolvedConfig.root, id.slice(1)))
+                        : normalizePath(id);
+                    const typescriptResult = fileEmitter(lookupId) ?? fileEmitter(id);
                     // File not in the Angular program — skip and let other plugins
                     // or Vite's built-in transform handle it. Warn if it looks like
                     // an Angular file that should have been compiled.
                     if (!typescriptResult) {
                         const isAngular = !id.includes('@ng/component') &&
+                            !id.includes('node_modules') &&
                             ANGULAR_DECORATOR_CALL_RE.test(code);
                         if (isAngular) {
                             this.warn(`[@analogjs/vite-plugin-angular]: "${id}" contains Angular decorators but is not in the TypeScript program. ` +
@@ -1069,15 +1074,20 @@ export function angular(options) {
             if (ids && ids.length > 0) {
                 ids.forEach((id) => writeOutputFile(id));
             }
+            else if (isTest) {
+                // TypeScript will loop until there are no more affected files in the program
+                while (builder.emitNextAffectedFile(writeFileCallback, undefined, undefined, transformers)) {
+                    /* empty */
+                }
+            }
             else {
-                /**
-                 * Only block the server from starting up
-                 * during testing.
-                 */
-                if (isTest) {
-                    // TypeScript will loop until there are no more affected files in the program
-                    while (builder.emitNextAffectedFile(writeFileCallback, undefined, undefined, transformers)) {
-                        /* empty */
+                // Emit all source files upfront in dev mode so that Vite can
+                // serve them on demand. The on-demand outputFile callback is
+                // assigned but never invoked by the plugin, so without this
+                // eager emit only the entry points are available.
+                for (const sourceFile of typeScriptProgram.getSourceFiles()) {
+                    if (!sourceFile.isDeclarationFile) {
+                        writeOutputFile(sourceFile.fileName);
                     }
                 }
             }
diff --git a/src/lib/router-plugin.js b/src/lib/router-plugin.js
index 3a7ca9e6549ff56ab9371ae6f3469fdb882277c3..34a048f7681b2620afae5f107887ca8f6d33669e 100644
--- a/src/lib/router-plugin.js
+++ b/src/lib/router-plugin.js
@@ -24,10 +24,15 @@ export function routerPlugin() {
         },
         transform: {
             filter: {
-                id: /fesm(.*?)\.mjs/,
+                id: {
+                    include: [/fesm(.*?)\.mjs$/],
+                },
             },
             async handler(_code, id) {
                 const path = id.split('?')[0];
+                if (!/fesm(.*?)\.mjs$/.test(path)) {
+                    return;
+                }
                 const contents = await javascriptTransformer.transformFile(path);
                 return {
                     code: Buffer.from(contents).toString('utf-8'),
